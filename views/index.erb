<ol class = "links">
  <% @articles.each do |article| %>
    <li>
      <p class=".arrow"> <img class = "arrow" src = "grayarrow.gif">
        <%= article["title"] %>
      </p>
      <p>
        <a href= <%= "#{article["url"]}" %> > <%= article["url"] %> </a>
      </p>
      <br />
      <p>
        <%= article["score"] %> points by
      </p>
      <p>
        <%= article["user_id"] %>
      </p>
      <p>
        <%= article["description"] %>
      </p>
    </li>
  <% end %>
</ol>
